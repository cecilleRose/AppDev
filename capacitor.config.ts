import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'first activity',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
